{% set name = "pyglet" %}
{% set version = "1.4.1" %}
{% set sha256 = "42cce50a97cf524a28d3176652353ca3a7e73fb46c093c1eba698df20822578c" %}

package:
    name: {{ name }}
    version: {{ version }}

source:
    fn: {{ name }}-{{ version }}.tar.gz
    url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
    sha256: {{ sha256 }}

build:
    number: 0
    script: "{{ PYTHON }} -m pip install . -vv"

requirements:
  build:
    - python
    - pip
  run:
    - python
    - freetype
    - future

test:
  requires:
    - libglu  # [linux]
  files:
    - run_tests.py
  commands:
    - python run_tests.py  # [not linux]
    - xvfb-run --server-args=":99 -screen 0 1024x768x24 -ac +extension GLX -noreset" python run_tests.py  # [linux]

about:
  home: https://pyglet.readthedocs.io/en/pyglet-1.3-maintenance/
  license: BSD-3-Clause
  license_family: BSD
  summary: 'Cross-platform windowing and multimedia library'

extra:
  recipe-maintainers:
    - mwcraig
    - jakirkham
    - r1chardj0n3s
    - silvester747
    - bitcraft
    - hoechenberger
